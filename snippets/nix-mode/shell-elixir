{ nixpkgsPath ? <nixpkgs>, erlangVersion ? "${1:R22}", elixirVersion ? "${2:1_10}" }:

with import nixpkgsPath {};

let
  erlang = beam.packages."erlang${erlangVersion}";
  elixir = erlang."elixir_${elixirVersion}";
in mkShell {
  buildInputs = [
    elixir
  ];
}
