{ nixpkgs ? import <nixpkgs> {}, jdkName ? "${1:openjdk}", jdkVersion ? "${2:8}" }:

with nixpkgs;

let
  jdk = pkgs."\${jdkName}\${jdkVersion}";
in mkShell {
  buildInputs = [
    leiningen
    jdk
  ];
}
