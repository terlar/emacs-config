{ nixpkgs ? import <nixpkgs> {}, scalaVersion ? "${1:2_13}" }:

with nixpkgs;

let
  scala = pkgs."scala_\${scalaVersion}";
in mkShell {
  buildInputs = [
    coursier
    sbt
    scala
    scalafmt
  ];
}
